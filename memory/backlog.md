# Starting backlog — day one

- First audit: signer + postgres verify tests vs README known gaps (gRPC, cross-service auto-emit, public key export / JWKS).
- Residual risk honesty: receipts prove integrity not detector correctness — document emitter coverage gaps from gateway/agent-gate/policy.
- Key history: confirm AEGIS_AUDIT_SIGNING_KEYS_HISTORY rotation path still covered by tests after recent rotate-script work.
